<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.chart.LineChart?>
<?import javafx.scene.chart.NumberAxis?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<ScrollPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.sparrowwallet.sparrow.gui.StatsController"
            stylesheets="@ashigaru.css"
            styleClass="wallet-panel"
            fitToWidth="true">
    <content>
        <VBox spacing="16" styleClass="verifier-panel">
            <padding><Insets top="22" right="22" bottom="22" left="22"/></padding>

            <!-- Hero -->
            <HBox spacing="16" alignment="CENTER_LEFT" styleClass="verifier-hero">
                <padding><Insets top="18" right="20" bottom="18" left="20"/></padding>
                <StackPane styleClass="verifier-hero-icon">
                    <Label text="📊" styleClass="verifier-hero-glyph"/>
                </StackPane>
                <VBox spacing="3" HBox.hgrow="ALWAYS">
                    <Label text="Whirlpool Statistics" styleClass="verifier-hero-title"/>
                    <Label text="Live pool sizes and mixing activity from whirlpoolstats.xyz."
                           wrapText="true" styleClass="verifier-hero-sub"/>
                </VBox>
                <Button fx:id="refreshBtn" text="Refresh" onAction="#onRefresh" styleClass="secondary-btn"/>
            </HBox>

            <!-- Loading / error status -->
            <Label fx:id="statusLabel" visible="false" managed="false" wrapText="true" styleClass="field-label"/>

            <!-- Summary cards -->
            <HBox spacing="12" alignment="CENTER_LEFT">
                <VBox fx:id="totalCard" spacing="6" styleClass="ag-card balance-card primary" HBox.hgrow="ALWAYS">
                    <padding><Insets top="16" right="16" bottom="16" left="16"/></padding>
                    <Label text="TOTAL BTC IN POOLS" styleClass="card-faint"/>
                    <Label fx:id="totalBtcLabel" text="—" styleClass="card-value"/>
                </VBox>
                <VBox spacing="6" styleClass="ag-card" HBox.hgrow="ALWAYS">
                    <padding><Insets top="16" right="16" bottom="16" left="16"/></padding>
                    <Label text="BLOCK RANGE" styleClass="card-faint"/>
                    <Label fx:id="blockRangeLabel" text="—" styleClass="card-value"/>
                </VBox>
                <VBox spacing="6" styleClass="ag-card" HBox.hgrow="ALWAYS">
                    <padding><Insets top="16" right="16" bottom="16" left="16"/></padding>
                    <Label text="LAST UPDATED" styleClass="card-faint"/>
                    <Label fx:id="lastUpdatedLabel" text="—" styleClass="card-value"/>
                </VBox>
            </HBox>

            <!-- Pool cards -->
            <HBox spacing="12">
                <VBox fx:id="pool025Card" spacing="6" styleClass="ag-card" HBox.hgrow="ALWAYS">
                    <padding><Insets top="16" right="16" bottom="16" left="16"/></padding>
                    <HBox alignment="CENTER_LEFT" spacing="8">
                        <Label text="0.025 BTC POOL" styleClass="card-faint"/>
                        <Region HBox.hgrow="ALWAYS"/>
                        <Label fx:id="pool025SizeBadge" text="—" styleClass="ag-chip accent"/>
                    </HBox>
                    <Label fx:id="pool025EnteredLabel" text="—" styleClass="card-secondary"/>
                    <Label fx:id="pool025CyclesLabel" text="—" styleClass="card-secondary"/>
                    <Label fx:id="pool025Tx0CountLabel" text="—" styleClass="card-faint"/>
                </VBox>
                <VBox fx:id="pool25Card" spacing="6" styleClass="ag-card" HBox.hgrow="ALWAYS">
                    <padding><Insets top="16" right="16" bottom="16" left="16"/></padding>
                    <HBox alignment="CENTER_LEFT" spacing="8">
                        <Label text="0.25 BTC POOL" styleClass="card-faint"/>
                        <Region HBox.hgrow="ALWAYS"/>
                        <Label fx:id="pool25SizeBadge" text="—" styleClass="ag-chip accent"/>
                    </HBox>
                    <Label fx:id="pool25EnteredLabel" text="—" styleClass="card-secondary"/>
                    <Label fx:id="pool25CyclesLabel" text="—" styleClass="card-secondary"/>
                    <Label fx:id="pool25Tx0CountLabel" text="—" styleClass="card-faint"/>
                </VBox>
            </HBox>

            <!-- Chart -->
            <VBox spacing="10" styleClass="ag-card">
                <padding><Insets top="16" right="16" bottom="16" left="16"/></padding>
                <Label text="Pool Capacity Over Block Height" styleClass="step-title"/>
                <LineChart fx:id="poolChart" animated="false" createSymbols="false" prefHeight="320" minHeight="280">
                    <xAxis><NumberAxis fx:id="xAxis" label="Block Height" /></xAxis>
                    <yAxis><NumberAxis fx:id="yAxis" label="Pool Size (BTC)" /></yAxis>
                </LineChart>
            </VBox>

            <!-- Recent Tx0s -->
            <VBox spacing="10" styleClass="ag-card">
                <padding><Insets top="16" right="16" bottom="16" left="16"/></padding>
                <Label text="Recent Tx0 Transactions" styleClass="step-title"/>
                <ListView fx:id="txListView" prefHeight="240" minHeight="200"/>
            </VBox>

            <!-- Footer link -->
            <HBox alignment="CENTER_LEFT" spacing="8">
                <Hyperlink fx:id="sourceLink" text="View full stats at whirlpoolstats.xyz" onAction="#onOpenSource"/>
            </HBox>
        </VBox>
    </content>
</ScrollPane>
